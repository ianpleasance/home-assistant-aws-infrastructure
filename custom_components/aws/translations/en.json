{
  "config": {
    "step": {
      "user": {
        "title": "AWS Infrastructure Monitor",
        "description": "Configure your AWS account for monitoring",
        "data": {
          "account_name": "Account Name",
          "aws_access_key_id": "AWS Access Key ID",
          "aws_secret_access_key": "AWS Secret Access Key",
          "region_mode": "Region Mode",
          "refresh_interval_minutes": "Refresh Interval (minutes)"
        }
      },
      "select_regions": {
        "title": "Select AWS Regions",
        "description": "Choose which regions to monitor",
        "data": {
          "regions": "Regions to Monitor"
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid AWS credentials. Please check your Access Key ID and Secret Access Key."
    },
    "abort": {
      "already_configured": "This AWS account is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "AWS Infrastructure Options",
        "description": "Update integration settings",
        "data": {
          "refresh_interval_minutes": "Refresh Interval (minutes)"
        }
      }
    }
  }
}
